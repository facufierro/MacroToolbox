# Hotkey Manager v1.6.8

## Changes

- Updating from within the app now applies the update quietly (progress bar only) instead of showing the installer's prompts — it no longer asks whether to delete your data and no longer re-adds a desktop shortcut, and it relaunches the app when done.
