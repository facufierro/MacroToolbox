# Hotkey Manager v3.2.0

## Changes

- Fixed the update banner not appearing when opening the app from the tray: the update check now also runs whenever the window is shown, instead of only at launch and on a timer that Windows throttles while the window is hidden.
- Added a "Check for Updates" button in Settings that checks immediately and reports either the available version or that the app is up to date.
