# Hotkey Manager v1.6.2

## Changes

- Fixed the app not opening at Windows login: tray-icon creation now retries (the taskbar is often not ready yet that early in login) and overlay-window creation no longer aborts startup, so a transient failure at login can no longer stop the app — including its hotkeys — from starting.
