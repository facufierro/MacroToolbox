# Hotkey Manager v3.2.3

## Changes

- Fixed hotkey triggers binding the wrong physical key on layouts that move letters around (e.g. QWERTZ): a trigger like "z" now fires on the key that actually types z on the active layout, and recording stores the character shown on the key. Layouts without the character (e.g. Cyrillic) keep the US-position binding.
