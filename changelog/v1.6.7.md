# Hotkey Manager v1.6.7

## Changes

- Fixed the app never launching on Windows startup. Because Hotkey Manager runs as administrator, Windows refuses to auto-start it from the startup registry entry (it can't show a UAC prompt at logon). It now registers a scheduled task that runs at logon with elevated privileges instead, and cleans up the old registry/Startup-folder entries that could never start it.
