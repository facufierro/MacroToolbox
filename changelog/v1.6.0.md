# Hotkey Manager v1.6.0

## Changes

- The app now runs as a single instance: launching it again while it is already open (including from the tray) just brings the existing window to the front instead of starting a second copy.
- When creating or editing a scope, the Executable field now has a dropdown of currently-open apps to pick from, in addition to typing the name manually.
