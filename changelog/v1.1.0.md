# Hotkey Manager v1.1.0

## Changes

- The "update available" banner can now update the app in place: click "Update Now" to download the latest installer and launch it automatically (after a confirmation prompt), or use "Release Notes" to open the release page. This replaces the old button that opened the installer's direct download link in the browser (which left a second, blank tab behind).
